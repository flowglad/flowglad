{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["BIOME_BINARY"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "build:declarations"],
      "outputs": ["dist/**"]
    },
    "check": {
      "dependsOn": ["^check", "^build:declarations"],
      "outputs": []
    },
    "build:declarations": {
      "dependsOn": ["^build:declarations"],
      "outputs": ["dist/types/**"]
    },
    "test": {
      "dependsOn": ["^test", "^build", "^build:declarations"],
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^dev"],
      "cache": false,
      "persistent": true
    }
  }
}
