node_modules
.DS_Store
.changeset-lock.json
.changeset-lock.yaml
.changeset-lock.yml

.env
.env.local
.env.development
.env.production
.env.test
.env.test.local

.yalc/
yalc.lock

dist

# TypeScript declaration files in source directories (should only be in dist/)
**/src/**/*.d.ts

generated/styles.ts
.turbo
.cursor/mcp.json

# used to temporarily backup development routes in case of accidental deletion
.dev-routes-backup
.ion-target
.vercel

# Package managers - ALWAYS use bun in this project
package-lock.json
yarn.lock
pnpm-lock.yaml

.workspace-deps-backup.json

database.sqlite
playground/react-native/prisma/database.sqlite

# Git worktrees
.conductor

# openapi diffing
temp-openapi**.json

/tags
