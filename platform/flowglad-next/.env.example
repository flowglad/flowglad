# Database connection (use this for Docker setup)
DATABASE_URL=postgresql://flowglad:flowglad_dev_password@localhost:5432/flowglad_db

STRIPE_SECRET_KEY=sk_live_stub
STRIPE_TEST_MODE_SECRET_KEY=sk_test_stub
UNKEY_ROOT_KEY=unkey_root_stub
UNKEY_API_ID=api_stub
NEXT_PUBLIC_APP_URL=http://localhost:3000
SVIX_API_KEY=svix_api_stub
DEV_EMAIL_REDIRECT=your-email@domain.com #This is the email address that all emails will be sent to in local development

# Trace sampling rate (0.0-1.0, default 1.0 = 100% of traces)
# Use lower values in production to reduce costs, e.g. 0.1 = 10%
FLOWGLAD_TRACE_SAMPLE_RATE=1.0
