/**
 * Shared font constants for email templates.
 *
 * These fonts are 100% email client compatible with zero external dependencies.
 * - Headings use Georgia (serif) for a classic, professional look
 * - Body text uses system font stack for optimal readability across platforms
 */

/** Georgia serif font for headings */
export const HEADING_FONT_FAMILY = 'Georgia, "Times New Roman", serif'

/** System font stack for body text - cross-platform compatible */
export const BODY_FONT_FAMILY =
  '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif'
