/**
 * Cookie prefix constants for merchant and customer auth sessions.
 * These prefixes ensure that merchant and customer sessions can coexist
 * in the same browser without collision.
 */
export const MERCHANT_COOKIE_PREFIX = 'merchant.'
export const CUSTOMER_COOKIE_PREFIX = 'customer.'
