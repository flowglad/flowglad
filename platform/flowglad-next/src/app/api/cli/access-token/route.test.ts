import { describe, it } from 'bun:test'

describe('POST /api/cli/access-token', () => {
  it.skip('creates Unkey key with correct metadata', () => {
    // PENDING: Patch 5
  })

  it.skip('validates user has access to organization', () => {
    // PENDING: Patch 5
  })

  it.skip('validates pricing model belongs to organization', () => {
    // PENDING: Patch 5
  })

  it.skip('returns 401 for invalid session', () => {
    // PENDING: Patch 5
  })
})
