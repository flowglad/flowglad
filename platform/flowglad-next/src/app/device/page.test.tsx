/**
 * @vitest-environment jsdom
 */
import { describe, it } from 'bun:test'

describe('device redirect', () => {
  it.skip('redirects from /device to /cli/authorize with user_code', () => {
    // PENDING: Patch 8
  })
})
