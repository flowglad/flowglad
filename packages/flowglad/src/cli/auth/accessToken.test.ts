import { describe, it } from 'vitest'

describe('requestAccessToken', () => {
  it.skip('returns Unkey API key with expiry', () => {
    // PENDING: Patch 5
  })
})

describe('isAccessTokenExpired', () => {
  it.skip('returns true for expired token', () => {
    // PENDING: Patch 5
  })

  it.skip('returns false for valid token', () => {
    // PENDING: Patch 5
  })
})
