import { describe, it } from 'vitest'

describe('saveCredentials', () => {
  it.skip('writes credentials file with 600 permissions', () => {
    // PENDING: Patch 3
  })
})

describe('loadCredentials', () => {
  it.skip('returns null when no credentials file exists', () => {
    // PENDING: Patch 3
  })

  it.skip('returns parsed credentials when file exists', () => {
    // PENDING: Patch 3
  })
})

describe('clearCredentials', () => {
  it.skip('removes credentials file', () => {
    // PENDING: Patch 3
  })
})
