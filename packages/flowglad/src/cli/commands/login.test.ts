import { describe, it } from 'vitest'

describe('login command', () => {
  it.skip('displays verification URL and user code', () => {
    // PENDING: Patch 6
  })

  it.skip('shows already logged in message when credentials exist', () => {
    // PENDING: Patch 6
  })
})
